<!--
ОТВЕЧАЕТ НА: что сейчас работает, что в процессе, известные проблемы и блокеры.
СЮДА НЕ ПИСАТЬ: историю изменений — CHANGELOG.md; план — ROADMAP.md; устройство чего-либо — профильные docs.
ИСТОЧНИК ПРАВДЫ: этот файл.
ОБНОВЛЯТЬ КОГДА: после каждой значимой задачи — перезаписью, не длиннее 50 строк.
-->

# Статус проекта

> Обновлено: 2026-08-19 | История — `CHANGELOG.md` | Фаза: 1 «Конвейер данных» | Прод: ещё не выкатывался

## Работает

- Развёрнута система работы с Claude Code: `docs/` с контракт-шапками, `.claude/CLAUDE.md`, `settings.json`, три агента, git-процесс с gitleaks.
- Репозиторий `DmitriyLavrisiuk/palworld-technology-tree` создан (публичный), `main` запушен, pre-commit отрабатывает на каждом коммите.
- GitHub MCP подключён и проверен в деле — репозиторий создан через него.

## В процессе

- Ничего. Следующая задача — Фаза 1: перенести парсер из прототипа и убедиться, что `npm run scrape` даёт 588 технологий с сошедшимися контрольными суммами.

## Известные проблемы

- Нет.

## Ожидаемо не работает (кода ещё нет)

- Команды `npm run dev / build / typecheck / scrape / deploy` объявлены в `package.json`, но зависимостей нет — приложение появится в Фазе 2, парсер в Фазе 1.
- Хук `PostToolUse` (`oxlint --fix`) молча не срабатывает, пока не установлен `node_modules`. Это задумано, а не сломано.

## Заблокировано

- Нет.
