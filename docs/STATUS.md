<!--
ОТВЕЧАЕТ НА: что сейчас работает, что в процессе, известные проблемы и блокеры.
СЮДА НЕ ПИСАТЬ: историю изменений — CHANGELOG.md; план — ROADMAP.md; устройство чего-либо — профильные docs.
ИСТОЧНИК ПРАВДЫ: этот файл.
ОБНОВЛЯТЬ КОГДА: после каждой значимой задачи — перезаписью, не длиннее 50 строк.
-->

# Статус проекта

> Обновлено: 2026-08-19 | История — `CHANGELOG.md` | Фаза: 2 «Каркас приложения и деплой» | Прод: ещё не выкатывался

## Работает

- **Система работы**: `docs/` (10 файлов с контракт-шапками), `.claude/CLAUDE.md`, настройки и три агента, pre-commit с gitleaks, GitHub MCP.
- Репозиторий `DmitriyLavrisiuk/palworld-technology-tree` — публичный, `main` опубликован.
- **Конвейер данных перенесён и работает**: `npm run scrape` собирает 588 технологий, 527 рецептов, 66 цепочек и 588 иконок. Все контрольные суммы сошлись; `technologies.json` и `recipes.json` побайтно совпадают с прототипом.
- `npm run typecheck` и `npm run lint` проходят чисто.

## В процессе

- Ничего. Следующая задача — Фаза 2: поднять Vite + React 19 + Tailwind v4 + shadcn/ui, завести загрузку данных чанками и выкатить первую версию на Pages.

## Известные проблемы

- **20 из 66 цепочек держатся на эвристике** (`stem` и `name`), покрытие узлов цепочками — 43%. Помечены в данных полем `confidence`; вычитка — Фаза 7.

## Ожидаемо не работает (кода приложения ещё нет)

- `npm run dev`, `npm run build`, `npm run deploy` — Vite и React появятся в Фазе 2. Установлены только зависимости парсера.
- `tsconfig.json` ссылается только на `tsconfig.node.json`; `tsconfig.app.json` заведётся в Фазе 2.

## Заблокировано

- Нет.
