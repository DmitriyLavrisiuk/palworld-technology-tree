# Зависимости и сборка
node_modules
dist
dist-ssr
*.local

# Логи
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Кэш парсера — скачанные страницы источников, восстанавливается запуском scrape
.cache/

# Редакторы и ОС
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Токену не хватает scope `workflow` — workflow-файлы не пушатся.
# Деплой идёт командой `npm run deploy`. См. docs/DEPLOYMENT.md.
.github/workflows/

# Личные настройки Claude Code, не общие для проекта
.claude/settings.local.json

# Отчёты аудита безопасности — локальные, содержат карту слабых мест
security/
